# Using Flutter Gradle Plugin, this line is required for this to work.
org.gradle.jvmargs=-Xmx4G

# The Flutter Gradle Plugin will handle the transitive dependencies
# of the Flutter SDK, the Flutter plugins, and packages that depend on packages
# that depend on the Flutter SDK.
